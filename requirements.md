# 日语单词小笔记本需求文档

版本：v0.1  
日期：2026-05-24  
角色定位：轻量级、手动维护的日语单词笔记本

## 1. 项目概述

本项目是一个面向个人学习者的轻量级日语单词笔记本。它的核心目标不是做成大型词典、AI 学习平台或复杂背词软件，而是提供一个简单、清晰、好维护的工具，帮助用户手动记录日语单词、假名读音、中文释义、备注和例句。

第一版强调“快速记录”和“方便回看”。用户可以把课堂、教材、视频、文章中遇到的词手动加入笔记本，并在之后通过搜索、筛选、详情页和备注内容进行复习。

## 2. 产品定位

### 2.1 一句话描述

一个可以手动记录日语单词、中文意思、读音、备注和例句的轻量化网页笔记本。

### 2.2 目标用户

- 正在学习日语的中文用户。
- 想把零散单词整理成个人词库的学习者。
- 更偏好手动输入和个人理解记录，而不是完全依赖 AI 自动生成内容的用户。
- 需要一个简单 UI 来代替纸质笔记本、备忘录或零散表格的用户。

### 2.3 核心价值

- 快速添加单词，不被复杂字段打断。
- 每个单词都可以记录自己的理解、使用场景和例句。
- 可以通过搜索和标签快速找回以前记录的单词。
- 数据结构清晰，后续可以继续扩展为复习、导入导出或 AI 辅助功能。

## 3. 当前阶段范围

### 3.1 第一版必须包含

- 单词新增、查看、编辑、删除。
- 单词列表。
- 搜索功能。
- 标签功能。
- 备注功能。
- 例句功能。
- 简单的学习状态标记。
- 本地保存数据。
- 简洁可用的 UI。

### 3.2 第一版暂不包含

- AI 自动生成解释、例句或翻译。
- 用户登录和云同步。
- 多设备同步。
- 复杂间隔重复算法。
- 发音音频播放。
- OCR 图片识别。
- 与 Anki、Notion 等外部工具的实时同步。

### 3.3 后续可扩展方向

- 导入 / 导出 CSV 或 JSON。
- 简单复习模式。
- AI 辅助生成例句。
- 词汇统计图表。
- 移动端 PWA 离线安装。
- 假名自动标注。

## 4. 设计原则

1. 轻量优先：界面和交互都应服务于快速记录，不做不必要的复杂化。
2. 手动可信：第一版所有词条内容由用户自己输入和维护。
3. 本地优先：数据默认保存在浏览器本地，打开即可使用。
4. 可扩展：字段和数据结构要为后续导入导出、复习和 AI 功能留出空间。
5. 中文友好：界面文案面向中文用户，日语字段清晰可辨。
6. 低干扰：添加单词时只要求最少必要信息，其余内容可以之后补充。

## 5. 信息架构

第一版建议采用单页应用结构，包含以下主要区域：

- 首页 / 仪表盘
- 单词列表
- 单词详情
- 新增 / 编辑单词表单
- 标签筛选区域

如果项目规模较小，也可以把这些区域放在同一个页面中完成：

- 左侧：标签和筛选。
- 中间：单词列表。
- 右侧或弹窗：词条详情与编辑。

## 6. 页面需求

## 6.1 首页

### 目标

让用户打开应用后能立即知道自己的词库情况，并快速添加新单词。

### 页面内容

- 应用标题：日语单词小笔记本。
- 快速添加按钮。
- 总单词数量。
- 最近添加的单词列表。
- 学习状态统计：
  - 未学
  - 学习中
  - 已掌握
- 最近使用的标签。

### 交互要求

- 点击“添加单词”进入新增表单或打开新增弹窗。
- 点击最近添加的单词进入词条详情。
- 点击某个状态统计，可以筛选对应状态的单词。

### 验收标准

- 用户打开首页后，可以在 3 秒内找到添加入口。
- 首页能显示当前已保存的单词总数。
- 当没有任何单词时，应显示友好的空状态，并引导用户添加第一个单词。

## 6.2 单词列表页

### 目标

展示所有已记录的单词，并支持搜索、筛选和快速进入详情。

### 列表项展示字段

每个单词卡片或列表项至少显示：

- 日语单词。
- 假名读音。
- 中文意思。
- 标签。
- 学习状态。
- 是否星标。

### 搜索能力

搜索框应支持搜索以下内容：

- 日语单词。
- 假名读音。
- 中文意思。
- 备注。
- 例句。
- 标签。

### 筛选能力

应支持按以下条件筛选：

- 标签。
- 学习状态。
- 是否星标。

### 排序能力

第一版至少支持：

- 最近添加优先。
- 最近编辑优先。
- 按日语单词排序。

### 交互要求

- 点击列表项进入词条详情。
- 搜索结果应实时更新或点击搜索按钮后更新。
- 无结果时应显示“没有找到匹配的单词”。
- 筛选条件应可以清除。

### 验收标准

- 添加新单词后，列表能立即显示该词。
- 搜索中文意思可以找到对应单词。
- 搜索假名可以找到对应单词。
- 标签筛选后，只显示拥有该标签的单词。

## 6.3 新增单词页 / 弹窗

### 目标

让用户快速录入一个新单词，并允许补充备注和例句。

### 字段要求

#### 必填字段

- 日语单词。
- 中文意思。

#### 选填字段

- 假名读音。
- 词性。
- 标签。
- 学习状态。
- 星标。
- 备注。
- 例句。

### 字段说明

#### 日语单词

- 用于记录汉字、假名或混合写法。
- 示例：勉強、食べる、きれい、ありがとう。

#### 假名读音

- 用于记录读音。
- 示例：べんきょう、たべる。

#### 中文意思

- 支持输入一个或多个含义。
- 第一版可以使用普通文本输入。
- 示例：学习；吃；漂亮、干净。

#### 词性

建议提供下拉选择：

- 名词
- 动词
- い形容词
- な形容词
- 副词
- 助词
- 连体词
- 接续词
- 感叹词
- 表达
- 其他

#### 标签

- 用户可手动输入标签。
- 一个单词可以有多个标签。
- 示例：N5、课本第1课、旅行、动词、容易混淆。

#### 学习状态

第一版提供三个状态：

- 未学
- 学习中
- 已掌握

默认状态为“未学”。

#### 星标

- 用于标记重点词。
- 默认为未星标。

#### 备注

用于记录用户自己的理解，例如：

- “这个词比另一个词更口语。”
- “老师说作文里常用。”
- “和 見える 容易混淆。”

#### 例句

每个例句建议包含：

- 日语例句。
- 中文翻译。
- 例句备注。

第一版至少允许记录一条例句；建议支持添加多条例句。

### 表单交互

- 必填字段为空时，不允许保存。
- 保存成功后返回列表或进入详情页。
- 用户取消时不保存当前更改。
- 编辑已有词条时，表单应自动填入已有内容。

### 验收标准

- 只填写“日语单词”和“中文意思”即可成功保存。
- 未填写日语单词时，应提示用户补全。
- 未填写中文意思时，应提示用户补全。
- 保存后刷新页面，数据仍然存在。

## 6.4 单词详情页

### 目标

让用户集中查看和维护一个词条的完整信息。

### 展示内容

- 日语单词。
- 假名读音。
- 中文意思。
- 词性。
- 标签。
- 学习状态。
- 星标状态。
- 备注。
- 例句列表。
- 创建时间。
- 最近更新时间。

### 操作能力

- 编辑词条。
- 删除词条。
- 切换学习状态。
- 切换星标。
- 新增例句。
- 编辑例句。
- 删除例句。

### 删除确认

删除词条前必须二次确认，避免误删。

确认文案建议：

> 确定要删除这个单词吗？删除后无法恢复。

### 验收标准

- 从列表点击单词可以进入详情。
- 详情页展示的信息与新增时填写的信息一致。
- 修改学习状态后，列表中的状态同步更新。
- 删除词条后，该词不再出现在列表中。

## 6.5 标签管理

### 目标

帮助用户按教材、等级、场景或个人分类整理词汇。

### 第一版需求

- 用户可以在新增 / 编辑单词时添加标签。
- 列表页可以按标签筛选。
- 标签可以在词条详情中展示。
- 首页或侧边栏可以展示常用标签。

### 标签规则

- 同一个单词可以有多个标签。
- 标签名称前后空格应自动去除。
- 空标签不应保存。
- 重复标签应自动合并。

### 验收标准

- 添加标签后，列表中可以看到该标签。
- 点击标签筛选时，只出现包含该标签的单词。
- 删除某个词条后，如果该标签没有被其他词使用，可以不再展示在常用标签中。

## 6.6 备注功能

### 目标

让用户记录自己的理解、易错点、使用场景和学习心得。

### 功能要求

- 每个词条拥有一个备注字段。
- 备注支持多行文本。
- 备注可以为空。
- 备注内容应参与搜索。

### 推荐使用方式

备注可以记录：

- 词义辨析。
- 使用场景。
- 常见搭配。
- 易错点。
- 老师补充。
- 自己的记忆方法。

### 验收标准

- 用户可以为任意词条添加备注。
- 修改备注后刷新页面，备注仍然存在。
- 搜索备注中的关键词，可以找到对应词条。

## 6.7 例句功能

### 目标

让用户不仅记录单词含义，也能记录实际使用方式。

### 例句字段

每条例句包含：

- 日语例句。
- 中文翻译。
- 例句备注。

### 功能要求

- 一个单词可以有多条例句。
- 可以新增、编辑、删除例句。
- 日语例句为建议必填，中文翻译和例句备注可选。
- 例句内容应参与搜索。

### 示例

单词：勉強する  
日语例句：毎日日本語を勉強しています。  
中文翻译：我每天都在学习日语。  
例句备注：用于表达正在持续学习某件事。

### 验收标准

- 用户可以给一个单词添加多条例句。
- 删除某条例句不会删除整个词条。
- 搜索例句中的日语或中文内容，可以找到对应词条。

## 7. 功能需求清单

| 编号 | 功能 | 优先级 | 说明 |
| --- | --- | --- | --- |
| FR-001 | 新增单词 | P0 | 用户可以创建一个新的单词词条 |
| FR-002 | 编辑单词 | P0 | 用户可以修改已有词条内容 |
| FR-003 | 删除单词 | P0 | 用户可以删除词条，删除前需确认 |
| FR-004 | 查看单词列表 | P0 | 用户可以看到全部词条 |
| FR-005 | 查看单词详情 | P0 | 用户可以查看完整词条信息 |
| FR-006 | 本地保存 | P0 | 数据刷新后不丢失 |
| FR-007 | 搜索单词 | P0 | 支持按单词、假名、中文意思、备注、例句搜索 |
| FR-008 | 标签记录 | P0 | 词条可以添加多个标签 |
| FR-009 | 标签筛选 | P0 | 用户可以按标签筛选词条 |
| FR-010 | 备注记录 | P0 | 每个词条可以记录多行备注 |
| FR-011 | 例句记录 | P0 | 每个词条可以记录一条或多条例句 |
| FR-012 | 学习状态 | P1 | 用户可以标记未学、学习中、已掌握 |
| FR-013 | 星标重点词 | P1 | 用户可以收藏重点词 |
| FR-014 | 排序 | P1 | 支持最近添加、最近编辑、日语单词排序 |
| FR-015 | 常用标签展示 | P1 | 首页或侧边栏展示当前标签 |
| FR-016 | 空状态提示 | P1 | 无数据或无搜索结果时给出清晰提示 |
| FR-017 | 导出数据 | P2 | 后续支持导出 JSON 或 CSV |
| FR-018 | 导入数据 | P2 | 后续支持从 JSON 或 CSV 导入 |
| FR-019 | 简单复习模式 | P2 | 后续支持按状态或星标复习 |
| FR-020 | AI 辅助例句 | P3 | 后续可选，不属于第一版范围 |

优先级说明：

- P0：第一版必须实现。
- P1：第一版建议实现。
- P2：后续版本实现。
- P3：远期可选功能。

## 8. 数据模型建议

### 8.1 Word 单词对象

```ts
type Word = {
  id: string;
  term: string;
  reading?: string;
  meanings: string;
  partOfSpeech?: PartOfSpeech;
  tags: string[];
  status: StudyStatus;
  starred: boolean;
  notes?: string;
  examples: ExampleSentence[];
  createdAt: string;
  updatedAt: string;
};
```

### 8.2 ExampleSentence 例句对象

```ts
type ExampleSentence = {
  id: string;
  japanese: string;
  chinese?: string;
  note?: string;
  createdAt: string;
  updatedAt: string;
};
```

### 8.3 StudyStatus 学习状态

```ts
type StudyStatus = "new" | "learning" | "mastered";
```

显示文案：

| 状态值 | 中文显示 |
| --- | --- |
| new | 未学 |
| learning | 学习中 |
| mastered | 已掌握 |

### 8.4 PartOfSpeech 词性

```ts
type PartOfSpeech =
  | "noun"
  | "verb"
  | "i_adjective"
  | "na_adjective"
  | "adverb"
  | "particle"
  | "adnominal"
  | "conjunction"
  | "interjection"
  | "expression"
  | "other";
```

## 9. 本地存储需求

### 9.1 存储方式

第一版可使用浏览器 `localStorage` 保存数据。

建议 key：

```txt
japanese-vocab-notebook.words
```

### 9.2 存储要求

- 每次新增、编辑、删除后都应更新本地存储。
- 应用启动时从本地存储读取数据。
- 本地存储不存在数据时，应初始化为空数组。
- 数据解析失败时，应避免页面崩溃，并提示用户数据异常。

### 9.3 后续扩展

如果数据量增大，可以迁移到 IndexedDB，但第一版不需要。

## 10. 交互细节

### 10.1 新增流程

1. 用户点击“添加单词”。
2. 系统展示新增表单。
3. 用户填写日语单词和中文意思。
4. 用户可选填读音、词性、标签、备注和例句。
5. 用户点击“保存”。
6. 系统校验必填字段。
7. 保存成功后，回到列表并展示新词条。

### 10.2 编辑流程

1. 用户从列表进入详情。
2. 点击“编辑”。
3. 系统展示已有信息。
4. 用户修改字段。
5. 点击“保存”。
6. 系统更新 `updatedAt`。
7. 页面展示最新内容。

### 10.3 删除流程

1. 用户进入词条详情。
2. 点击“删除”。
3. 系统弹出确认提示。
4. 用户确认后删除词条。
5. 系统返回列表。

### 10.4 搜索流程

1. 用户在搜索框输入关键词。
2. 系统匹配词条中的单词、读音、意思、标签、备注和例句。
3. 列表展示匹配结果。
4. 用户清空搜索框后，恢复全部列表。

### 10.5 标签筛选流程

1. 用户点击某个标签。
2. 系统只展示包含该标签的词条。
3. 用户点击“清除筛选”后，恢复全部列表。

## 11. UI 需求

### 11.1 整体风格

- 干净、安静、适合学习。
- 避免过度装饰。
- 优先保证信息清晰和操作直接。
- 桌面端应有较好的信息密度。
- 移动端应保证表单和列表可用。

### 11.2 布局建议

桌面端：

- 顶部：应用标题、搜索框、添加按钮。
- 左侧：标签和状态筛选。
- 中间：单词列表。
- 右侧：词条详情或编辑区域。

移动端：

- 顶部：标题、搜索框、添加按钮。
- 主体：单词列表。
- 详情和编辑采用独立页面或全屏弹窗。
- 标签筛选可以折叠。

### 11.3 视觉层级

- 日语单词应是列表项和详情页中最醒目的内容。
- 假名读音放在日语单词附近，字号略小。
- 中文意思应易读。
- 标签使用轻量标记样式。
- 学习状态使用清晰但不刺眼的颜色区分。

### 11.4 响应式要求

- 页面宽度小于 768px 时，切换为单列布局。
- 表单输入框宽度适配屏幕。
- 按钮和可点击区域在移动端应足够大。
- 文本不能溢出容器。

## 12. 表单校验与错误提示

### 12.1 校验规则

- 日语单词不能为空。
- 中文意思不能为空。
- 标签中的空字符串应自动移除。
- 重复标签应去重。
- 例句如果完全为空，不应保存为一条有效例句。

### 12.2 错误提示

错误提示应靠近对应字段展示。

建议文案：

- 请输入日语单词。
- 请输入中文意思。
- 例句内容为空，无法保存。

### 12.3 成功提示

可选。保存成功后可以轻量提示：

- 已保存。
- 已更新。
- 已删除。

## 13. 空状态与边界情况

### 13.1 无单词

文案建议：

> 还没有记录任何单词。先添加第一个日语单词吧。

应提供“添加单词”按钮。

### 13.2 搜索无结果

文案建议：

> 没有找到匹配的单词。

应提供“清空搜索”操作。

### 13.3 标签无结果

文案建议：

> 这个标签下暂时没有单词。

### 13.4 本地数据异常

文案建议：

> 本地数据读取失败，请检查或重置数据。

第一版可以只在控制台输出错误，同时页面不崩溃。

## 14. 非功能需求

### 14.1 性能

- 500 条以内词条搜索和筛选应流畅。
- 页面首屏加载应尽量快速。
- 本地存储读写不应明显阻塞界面。

### 14.2 可用性

- 常用操作不超过 2 次点击。
- 新增单词流程应尽量短。
- 编辑和删除入口清晰可见。
- 删除必须二次确认。

### 14.3 可维护性

- 单词数据结构应集中定义。
- 本地存储读写应封装为独立模块。
- 搜索和筛选逻辑应独立于 UI，便于测试。
- 表单校验逻辑应可复用。

### 14.4 可访问性

- 输入框必须有明确标签。
- 按钮文案应明确。
- 键盘可以完成基本表单输入和提交。
- 颜色不能作为区分状态的唯一方式，应配合文字。

## 15. 开发建议

### 15.1 技术选型

如果从零开发，建议使用：

- React 或 Vue。
- TypeScript。
- Vite。
- 本地存储使用 `localStorage`。
- 样式可以使用 CSS Modules、普通 CSS 或轻量 UI 组件。

如果希望最轻量，也可以使用：

- 原生 HTML + CSS + JavaScript。

但为了后续维护和扩展，建议使用 TypeScript + 组件化框架。

### 15.2 推荐模块划分

```txt
src/
  components/
    WordForm.tsx
    WordList.tsx
    WordDetail.tsx
    TagFilter.tsx
    SearchBar.tsx
  data/
    storage.ts
  types/
    word.ts
  utils/
    search.ts
    tags.ts
    date.ts
  App.tsx
```

### 15.3 状态管理

第一版不需要复杂状态管理库。可以使用：

- React `useState`。
- React `useMemo`。
- React `useEffect`。

当功能变复杂后，再考虑引入 Zustand、Redux 或其他方案。

## 16. 测试与验收

### 16.1 手动验收场景

#### 场景一：新增最简单词条

1. 打开应用。
2. 点击“添加单词”。
3. 输入日语单词：勉強。
4. 输入中文意思：学习。
5. 点击保存。
6. 回到列表。

预期结果：

- 列表出现“勉強”。
- 刷新页面后“勉強”仍然存在。

#### 场景二：新增带备注和例句的词条

1. 添加单词：食べる。
2. 假名读音：たべる。
3. 中文意思：吃。
4. 标签：N5、动词。
5. 备注：常用的一段动词。
6. 例句：朝ご飯を食べます。
7. 保存。

预期结果：

- 详情页能看到所有输入内容。
- 搜索“朝ご飯”能找到该词。
- 点击标签“N5”能看到该词。

#### 场景三：编辑词条

1. 进入“食べる”详情。
2. 点击编辑。
3. 修改备注。
4. 保存。

预期结果：

- 详情页显示新备注。
- 刷新后新备注仍存在。
- 更新时间发生变化。

#### 场景四：删除词条

1. 进入某个词条详情。
2. 点击删除。
3. 确认删除。

预期结果：

- 词条从列表消失。
- 刷新页面后仍然不存在。

#### 场景五：搜索和筛选

1. 添加多个不同标签的词条。
2. 搜索中文意思。
3. 搜索假名。
4. 点击标签筛选。

预期结果：

- 搜索结果准确。
- 标签筛选结果准确。
- 清除筛选后恢复全部列表。

### 16.2 自动化测试建议

如果项目使用 TypeScript + React，建议至少测试：

- 标签去重逻辑。
- 搜索匹配逻辑。
- 本地存储读写逻辑。
- 必填字段校验逻辑。

## 17. 第一版完成标准

当以下条件全部满足时，可以认为第一版完成：

- 用户可以新增、编辑、删除单词。
- 每个单词可以保存日语、读音、中文意思、词性、标签、状态、星标、备注和例句。
- 用户可以在列表中查看所有单词。
- 用户可以搜索单词、意思、假名、备注和例句。
- 用户可以通过标签筛选词条。
- 刷新页面后数据不丢失。
- 空状态、无搜索结果和删除确认都有基本处理。
- 桌面端和手机宽度下均可正常使用。

## 18. 给 Codex 的开发指令摘要

请基于本文档开发一个轻量级日语单词小笔记本。第一版不要接入 AI，不要做用户登录，不要做云同步。重点完成手动记录、备注、例句、搜索、标签筛选和本地保存。

开发时优先保证：

1. 数据不会因为刷新页面丢失。
2. 添加单词流程简单清楚。
3. 备注和例句可以方便编辑。
4. 搜索和标签筛选准确。
5. UI 清爽、中文友好、移动端可用。

